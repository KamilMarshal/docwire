import {Nav, Slider} from "../containers";

function Binaries() {
    return (
        <div>
            <Nav />
            <h1>Binaries</h1>
            <Slider />
        </div>
    )
}

export default Binaries;